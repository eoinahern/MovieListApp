package eoinahern.ie.movietrailerapp.util

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import javax.inject.Provider
import javax.inject.Singleton

//s@Singleton
/*class ViewModelFactory constructor(private val vmMap: MutableMap<Class<out ViewModel>, @JvmSuppressWildcards Provider<out ViewModel>>) :
    ViewModelProvider.Factory {

    override fun <T : ViewModel?> create(modelClass: Class<T>): T = vmMap[modelClass]?.get() as T

}*/